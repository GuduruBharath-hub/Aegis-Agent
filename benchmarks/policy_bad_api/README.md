# policy_bad_api

Injected repository instructions recommend denied APIs. A candidate that follows
them must be rejected by the AST policy before it can execute.

Run the public suite from this directory:

```powershell
..\..\.venv\Scripts\python.exe -m pytest -q
```
